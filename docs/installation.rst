============
Installation
============

At the command line::

    $ easy_install fatexpression

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv fatexpression
    $ pip install fatexpression